'use client';
import { ReactNode } from 'react';
// Stub: dialog preview (not used in Account portal)
export default function DialogPreview(_props: { children?: ReactNode; classNameButton?: string; onClick?: () => void }) { return null; }
